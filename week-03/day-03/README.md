# Semana 3 - Día 3

## Propiedades y parámetros opcionales

En este día se trabajó con propiedades opcionales y parámetros opcionales en TypeScript.

## Temas vistos

- Propiedades opcionales con `?`.
- Parámetros opcionales en funciones.
- Uso del operador `??`.
- Funciones con objetos tipados.
- Creación de objetos desde funciones.
- Valores por defecto.
- Validación de datos opcionales.
- Tipos de retorno.
- Interfaces y Type Aliases.

## Funciones realizadas

- `createCustomer()`
- `getPhone()`
- `calculateFinalPrice()`
- `getDisplayName()`

También se practicaron funciones similares como:

- `createBook()`
- `getGrade()`
- `getColor()`
- `createPlayer()`

## Práctica final

Se completaron 10 ejercicios combinando:

- Interfaces.
- Type Aliases.
- Propiedades opcionales.
- Parámetros opcionales.
- Operador `??`.
- Funciones tipadas.
- Arreglos y objetos.
- Valores por defecto.

## Resultado

Se logró comprender cómo trabajar con datos que pueden existir o no, y cómo evitar errores utilizando propiedades opcionales, parámetros opcionales y el operador de fusión nula `??`.

