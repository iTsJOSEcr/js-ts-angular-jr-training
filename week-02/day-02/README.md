## Día 2

### Temas estudiados

- Optional Chaining (`?.`)
- Nullish Coalescing (`??`)

### Conceptos aprendidos

- Acceso seguro a propiedades anidadas.
- Evitar errores cuando una propiedad no existe.
- Asignar valores por defecto usando `??`.
- Diferencia entre `||` y `??`.
- Combinación de `?.` y `??`.

### Prácticas realizadas

- Función `getEmail()`
- Función `getPhone()`
- Función `getProfileName()`
- Función `getCity()`
- Ejercicios con objetos anidados.
- Valores por defecto para propiedades inexistentes.